# micromamba environment file for Date Idea Picker
name: datepicker_env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - kivy
  - kivy-garden
  - pip:
      - matplotlib
      - matplotlib-venn
      - pandas
# For Android integration, add briefcase, or chaquopy as needed
